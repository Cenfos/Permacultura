---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('posts'); // Verifica que esta línea obtiene la colección correcta
  return posts.map(post => ({
    params: { slug: post.slug },
  }));
}

const { slug } = Astro.params;
const posts = await getCollection('posts'); // Verifica que esta línea obtiene correctamente los posts

const post = posts.find(p => p.slug === slug);
if (!post) {
  throw new Error(`Post not found for slug: ${slug}`);
}
---

<Layout title={post.title}>
  <article>
    <h1>{post.title}</h1>
    <p>{post.body}</p>
  </article>
</Layout>

<style>
  article {
    max-width: 800px;
    margin: 0 auto;
  }
  h1 {
    color: var(--color-primary);
    margin-bottom: 1rem;
  }
</style>

<style>
  article {
    max-width: 800px;
    margin: 0 auto;
  }
  h1 {
    color: var(--color-primary);
    margin-bottom: 1rem;
  }
</style>
